name: "LiteLLM Proxy"
version: "1.0.0"
slug: "litellm_proxy"
description: "LiteLLM proxy with PostgreSQL backend"
arch:
  - amd64
startup: "application"
boot: "auto"
host_network: true
homeassistant_api: true